interface:
  display_name: "st"
  short_description: "Improve your request, then carry it out"
  default_prompt: "Use $st to improve this request into a clear, thorough ultra prompt, then immediately carry it out."
